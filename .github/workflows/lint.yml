name: Lint

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v4
      - name: Lint (strict)
        run: swiftlint lint --strict .
