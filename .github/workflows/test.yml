name: Test

on:
  pull_request:
  push:
    branches: main

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - run: |
          set -o pipefail
          swift test
